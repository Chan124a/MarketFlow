{
  "name": "marketflow",
  "version": "1.0.0",
  "description": "大盘数据看板 - A股/港股/美股",
  "private": true,
  "scripts": {
    "setup": "npm install && cd frontend && npm install && cd ../quant && uv sync && cd ../rust-backend && cargo fetch",
    "setup:legacy-backend": "cd backend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:quant\"",
    "dev:backend": "cd rust-backend && cargo run -- serve",
    "dev:backend:legacy": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:quant": "cd quant && uv run uvicorn app:app --reload --port 8000",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd rust-backend && cargo build --release",
    "build:backend:legacy": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd rust-backend && cargo run --release -- serve",
    "start:backend:legacy": "cd backend && npm run start:prod",
    "start:frontend": "cd frontend && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
